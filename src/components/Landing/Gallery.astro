<section class="section bg-white">
    <div class="container">
        <h2 class="section-title">Portfolio</h2>
        <div class="grid-3">
            {Array(9).fill(null).map((_, i) => (
                <div key={i} class="aspect-square bg-gray-100 rounded-lg">
                    <!-- Image placeholder -->
                </div>
            ))}
        </div>
    </div>
</section> 